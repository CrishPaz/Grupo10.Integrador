import { defineStore } from 'pinia';

export const useFacturacionStore = defineStore('facturacion', {
    state: () => ({
        // Estado inicial
    }),
    actions: {
        // Acciones y métodos
    },
    getters: {
        // Getters
    }
});
